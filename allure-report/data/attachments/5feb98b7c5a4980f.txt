pm.test("Status code is 400", function () {
    pm.response.to.have.status(400);
});



// Realizar as validações desejadas utilizando pm.test()
   // pm.test("Validação produto Pernambucanas x Data Vencimento", function () {
   //   const responseBody = pm.response.json();

   //   pm.expect(responseBody.idProduto).to.eql(pm.variables.get("idProduto"));
  //    pm.expect(responseBody.diaVencimento).to.eql(pm.variables.get("diaVencimento"));
      
      // Adicione outras validações necessárias aqui
  //  });
  