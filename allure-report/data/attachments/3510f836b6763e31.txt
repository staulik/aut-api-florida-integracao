// Obtém o valor da variável global "token"
//var token = pm.globals.get("token");

// Verifica se a variável "token" tem um valor
//if (token) {
    // Configura o cabeçalho de autenticação "Bearer Token"
  //  pm.request.headers.add({
       // key: 'Authorization',
  //      value: 'Bearer ' + token
//    });
//} else {
   // console.error("A variável global 'token' não está definida.");
//}