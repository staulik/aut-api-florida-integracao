pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

// Extrair o valor do campo "idCartao" do response
const responseJson = pm.response.json();
const idCartao = responseJson.id;
const idPessoa = responseJson.idPessoa;
const idConta = responseJson.idConta;
const idStatus = responseJson.idStatus;

// Exibir informações no console
console.log(`As informações do cartão
  ID Cartão: ${idCartao}
  relacionado ao idPessoa: ${idPessoa}
  IdConta: ${idConta}
  foram gravadas com o status: ${idStatus}.
  O cartão esta preparado para ser transacionado.`);