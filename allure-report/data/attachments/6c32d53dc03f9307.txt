pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

// Analisar a resposta JSON
var responseJson = pm.response.json();

// Verificar a presença dos campos na resposta
if (!responseJson.content || responseJson.content.length === 0) {
    console.log("Nenhum item na resposta.");
} else {
    var item = responseJson.content[0];

    // Exibir os valores dos campos, se presentes
    console.log("idConta:", item.idConta);
    console.log("idPessoa:", item.idPessoa);
    console.log("nome:", item.nome);
}
